@model IEnumerable<ContactManager.Models.Contact>
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Список контактов</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Contact" asp-action="Create" class="btn btn-primary"> Создать новый контакт</a>
        </div>
    </div>
</div>


@*@if (Model.Count() > 0)
            {*@
                <table class="table table-bordered" style="width:100%">
                    <thead>
                        <tr>
                            <th>
                                ФИО
                            </th>
                            <th>
                                День рождения
                            </th>
                            <th>
                                Должность
                            </th>
                            <th>
                                Действия
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var obj in Model)
                        {
                           <tr>
                               <td>@obj.First_Name @obj.Last_Name</td>
                               <td>@obj.Birthday.ToLongDateString()</td>
                               <td>@obj.Position</td>
                               <td class="text-center">
                                   <div class="w-75 btn-group">
                                       <a asp-controller="Contact" asp-route-Id="@obj.Id" asp-action="Edit" class="btn btn-success mx-1">Редактировать</a>
                                       <a asp-controller="Contact" asp-route-Id="@obj.Id" asp-action="Delete" class="btn btn-danger mx-1">Удалить</a>
                                   </div>
                               </td>
                           </tr>
                        }
                    </tbody>
                </table>
            @*}
else
            {
                <p>No data</p>
            }*@